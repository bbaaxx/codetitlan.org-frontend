import {
  stringToupper
} from 'cdtorgfe/helpers/string-toupper';

module('StringToupperHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = stringToupper(42);
  ok(result);
});
