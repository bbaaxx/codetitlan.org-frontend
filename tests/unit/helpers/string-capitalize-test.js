import {
  stringCapitalize
} from 'cdtorgfe/helpers/string-capitalize';

module('StringCapitalizeHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = stringCapitalize(42);
  ok(result);
});
