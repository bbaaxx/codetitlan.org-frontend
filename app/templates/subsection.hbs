<div class="section__index">
  <header>
    <h2>{{string-capitalize name}}</h2>
  </header>
  <p>{{description}}</p>
</div>

<section>
  {{#each article in model.articles}}
    {{article.title}}
  {{/each}}
</section>

{{outlet}}
